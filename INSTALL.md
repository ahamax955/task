# 音乐作品管理系统 - 安装指南

## 📦 依赖安装

### 方法一：使用pip安装（推荐）
```bash
pip install -r requirements.txt
```

### 方法二：逐个安装
```bash
pip install Flask==2.3.3
pip install Flask-MySQLdb==1.0.1
pip install mysqlclient==2.2.0
pip install Pillow==10.0.1
pip install requests==2.31.0
```

## 🚀 运行项目

1. **安装依赖**
   ```bash
   pip install -r requirements.txt
   ```

2. **配置数据库**
   - 确保MySQL数据库已启动
   - 检查app.py中的数据库配置参数

3. **启动应用**
   ```bash
   python app.py
   ```

4. **访问应用**
   - 打开浏览器访问：http://127.0.0.1:5000

## 📁 项目结构
```
├── app.py                 # 主应用文件
├── requirements.txt       # 依赖包列表
├── static/               # 静态文件
│   ├── uploads/          # 用户上传图片
│   └── chopin_nocturne_staff.png
├── pages/                # HTML页面
├── css/                  # 样式文件
├── js/                   # JavaScript文件
└── README.md            # 项目说明
```

## 🔧 开发环境要求
- Python 3.8+
- MySQL 5.7+ 或 8.0+
- 现代浏览器（Chrome, Firefox, Safari, Edge）

## 📝 注意事项
1. 确保MySQL服务正在运行
2. 检查数据库连接配置是否正确
3. 确保uploads文件夹有写入权限
4. 如果遇到MySQL连接问题，请检查用户名和密码